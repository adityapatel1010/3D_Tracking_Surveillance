tracker_type: bytetrack
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6
track_buffer: 60  # Increased from default 30 to 60 to handle occlusions/flicker
match_thresh: 0.8
fuse_score: True  # Required by updated Ultralytics
